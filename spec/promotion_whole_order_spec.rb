require "spec_helper"

describe PromotionWholeOrder do
  it "has a version number" do
    expect(PromotionWholeOrder::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
